<div class="post-detail">
  <h1><%= @post.title %></h1>
  <p class="timestamp"><%= l @post.created_at, format: :long %></p>
  <div class="content">
    <%= simple_format @post.content %>
  </div>
</div>

<%= link_to t('.back'), posts_path %>
